{
    "name": "action-slack-webhook",
    "version": "1.0.0",
    "description": "This action will perform a POST request to a given Slack webhook. This can be used to push both simple messages as well as more complex Block Kit based rich messages.",
    "main": "dist/action.js",
    "scripts": {
        "build": "ncc build action.js --license licenses.txt",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/liveintent/action-slack-webhook.git"
    },
    "keywords": [
        "slack",
        "action",
        "webhook"
    ],
    "homepage": "https://github.com/liveintent/action-slack-webhook#readme",
    "license": "MIT",
    "devDependencies": {
        "@vercel/ncc": "^0.28.6",
        "prettier": "2.3.1"
    },
    "dependencies": {
        "@actions/core": "^1.4.0",
        "@slack/webhook": "^6.0.0"
    }
}
